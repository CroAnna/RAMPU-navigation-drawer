import 'package:flutter/material.dart';

class Odabir1 extends StatelessWidget {
  const Odabir1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
      title: const Text("Odabir 1"),
    ));
  }
}
